# 每日蛋白打卡 v2

在现有 GitHub Pages 原生 HTML/CSS/JavaScript 页面上迭代，无框架变更、运行时第三方依赖、后端或账号。

## 文件

- `index.html`：沿用原页面外壳、基础样式与食材装饰，加载 v2 脚本。
- `diary.css`：餐次、双目标、食物表单、历史与手机适配。
- `data.js`：Food / DailyFoodRecord / DailyRecord 模型、存储、快照计算、迁移与统计。
- `app.js`：今天、添加食物、目标、历史及只读详情；页面内确认框。
- `tests/data.test.cjs`：Node 内置测试，无需安装依赖。
- `serve.cjs`：仅开发时使用的静态文件服务器；上线不运行 Node。
- `tests/fixture.js`：仅开发测试使用的合成旧数据与模拟日期；生产页面不加载。

## 运行与验证

在该目录运行 `node serve.cjs`，打开 `http://127.0.0.1:8766/daily-protein-checkin/`。

运行 `node --test tests/data.test.cjs` 执行回归测试。

运行 `node serve.cjs --fixture` 在独立测试源 `http://127.0.0.1:8767/daily-protein-checkin/` 检查浏览器迁移和跨日。该模式预置合成旧记录，并提供模拟下一天按钮，不改动正式网址的数据。不要把测试模式页面作为成品发布。

## 存储与迁移

v2 使用 `protein-diary:v2` 保存版本号、食物库、日期映射、默认目标及已迁移键列表。旧版 `protein-checkin:YYYY-MM-DD` 原始数据不会被删除或覆盖。

每条饮食记录包含 foodId、名称/类型/单位/基准份量/蛋白质/热量快照、食用量、计算结果、餐次、创建时间。已有记录的数量调整使用快照基准，不读取后来编辑过的食物库。

每天保存独立目标。目标修改只影响今天与默认设置。跨日按 Asia/Shanghai，15 秒检查并在窗口恢复时检查；任何写入都会重新核对日期。历史详情只读。

迁移按日期逐条执行；损坏项留在原位置并显示提示。旧版没有餐次，归入加餐并注明来源；旧热量为 null，不补造历史热量。旧版蛋白质目标使用原达标下限 100g，升级当天使用新默认目标 110g。旧版没有热量目标，历史目标记为 null。

加载新版数据失败时保留原始数据并禁止覆盖，可从页面导出原始备份。容量不足时显示保存失败；不会宣称保存成功。写入前读取最新状态，避免常规多标签页串行编辑覆盖，但不提供跨设备同步。

## 营养值与统计

原有 7 种食物不重复添加，保留蛋白质和单位：全麦面包仍是 4g/片，鸡胸肉仍是 11g/50g 包。所有新增默认热量为可编辑估算，并在食物库与记录中标识；建议按实际包装核对。

7/30 天统计包含今天，蛋白质按有记录天数平均；热量按热量完整的记录天数平均。未记录日期显示空缺，不当作零摄入。重置过的日期仍保留，作为明确的零记录计算。复制昨天会追加记录并保留源快照，同一天禁止重复复制（重置后也不解除复制标记）。

## 部署

将 `index.html`、`data.js`、`app.js`、`diary.css` 保持在同一目录，全部资源使用相对路径，兼容 GitHub Pages 项目子路径。页面区域切换不创建服务器路由，刷新回到今天，不会产生子页面 404。静态发布原子更新上述文件；测试脚本与说明可随源码保留，但不被首页加载。

无自定义网络请求，无用户数据上传。导出仅在当前设备生成 JSON 下载。
